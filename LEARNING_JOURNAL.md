# 用 AI 学习 Ray Marching — 学习笔记

> 这份笔记记录了我从零开始用 AI 辅助学习 Ray Marching 的完整过程。
> 每个 step 对应一个 git commit，可以用 `git log` 查看完整历史。

## 学习路径总览

```
Step 1: 最简场景 → "我能看到一个 3D 球了！"
Step 2: 布尔运算 → "原来用 min/max 就能做 CSG！"
Step 3: 综合场景 → "这看起来像一个真正的作品了"
```

## Step 1: 最简 Ray Marching

### 我问了什么
> "我想在 ShaderToy 上用 Ray Marching 画一个球，请给我最简代码并解释每一行"

### 我学到了什么
1. **SDF（Signed Distance Function）** 是描述 3D 形状的一种方式
   - 给空间中任意一点，返回到最近表面的距离
   - 球体的 SDF 就是 `距离球心的长度 - 半径`，极其直觉
2. **Ray Marching** 就是"小心翼翼地往前走"
   - 每步走的距离 = 当前位置的 SDF 值（保证不会穿模）
   - 走到 SDF ≈ 0 时就击中了物体
3. **法线** = SDF 的梯度方向
   - 用 6 次采样近似（中心差分），不需要解析公式
4. **Phong 光照** 三件套：环境光 + 漫反射 + 镜面高光

### 顿悟时刻 💡
> "SDF 的值就是'安全步进距离'——这就是为什么叫 Sphere Tracing（球追踪）！
> 在当前点画一个半径为 SDF 值的球，这个球内不可能有任何物体表面。"

### 我尝试修改的参数
- 球的半径：0.5 → 1.0（球变大了）
- 摄像机 Y 位置：1.0 → 3.0（俯视角度）
- 高光指数：32 → 4（高光变大变模糊）→ 128（高光变小变尖锐）

---

## Step 2: SDF 布尔运算

### 我问了什么
> "SDF 有什么强大的特性？能做哪些 mesh 做不了或者很难做的事？"

### 我学到了什么
1. **布尔运算只需要 min/max**
   - Union = min(a, b) — 直觉：只要在任一物体内部就算在内
   - Intersection = max(a, b) — 直觉：必须同时在两个物体内部
   - Subtraction = max(a, -b) — 直觉：在 a 内部但不在 b 内部
2. **Smooth Union** 可以让物体融合
   - 像液态金属一样的效果，只需一个简单公式
   - `k` 参数控制融合范围
3. **Soft Shadow** 的原理
   - 从击中点向光源发射射线
   - 射线越"擦过"其他物体，阴影越浓
   - `k * d / t` 这个比值决定软硬程度

### 顿悟时刻 💡
> "传统网格做 CSG（Constructive Solid Geometry）需要计算交线、处理拓扑，
> 非常复杂。但在 SDF 的世界里，这一切只需要 min() 和 max()。
> 数学表示的选择决定了问题的难度——这是一个深刻的工程智慧。"

### 和 AI 的关键对话
> 我："smooth union 的公式看起来很神秘，能用直觉解释吗？"
>
> AI："想象两个物体靠近时，它们的表面像橡皮泥一样开始变形融合。
> `h = clamp(0.5 + 0.5*(b-a)/k, 0, 1)` 其实就是在算'两个物体的影响力占比'。
> 当 a ≈ b（两个物体等距）时，h ≈ 0.5，两边各贡献一半——这就是融合最强的地方。
> 最后的 `- k*h*(1-h)` 是'融合补偿'，让接缝处向内凹陷，产生平滑过渡。"

---

## Step 3: 综合场景 "The Emergence"

### 我问了什么
> "如何让场景看起来更真实？专业的 ShaderToy 作品通常包含哪些视觉技巧？"

### 我学到了什么
1. **Ambient Occlusion（AO）** 让凹陷处自然变暗
   - 原理：沿法线方向采样，如果 SDF 值比预期小，说明附近有遮挡
   - 只需 5 次采样就能大幅提升真实感
2. **Fresnel 反射** 让边缘更亮
   - 真实世界中，你从掠射角看任何表面，反射率都会增加
   - 公式：`pow(1.0 - NdotV, 5.0)`
3. **Tone Mapping** 防止过曝
   - Reinhard：`color / (1 + color)`，简单有效
4. **距离雾 + 暗角** 提升画面氛围

### 顿悟时刻 💡
> "AO 的实现竟然和 SDF 完美契合——因为 SDF 本身就是距离信息！
> 在 mesh-based 渲染中，AO 需要屏幕空间后处理（SSAO）或者预烘焙。
> 但在 SDF 中，5 行代码就搞定了。这再次证明：好的数学抽象让一切变简单。"

---

## 方法论总结：AI 共学的三条原则

### 原则 1: 先要画面，再要原理（Top-down）
不要从线性代数和渲染方程开始。先告诉 AI "我想画 X"，拿到能跑的代码，
在视觉反馈中发现问题和好奇心，然后带着具体问题去深入。

### 原则 2: 把 AI 当苏格拉底，不当搜索引擎
- ❌ "什么是 SDF"
- ✅ "我的代码渲染出来的球体边缘有锯齿，这和 Ray Marching 的步进精度有什么关系？"
- 好问题 = 具体场景 + 你的观察 + 你的困惑

### 原则 3: 改参数 → 预测结果 → 验证理解
学到一个概念后，改一个参数，先预测画面会怎么变，再跑代码验证。
如果预测错了，说明理解有误——这就是最高效的学习反馈循环。

---

## 文件说明

| 文件 | 说明 | ShaderToy 可直接运行 |
|------|------|---------------------|
| `step1_basic_sphere.glsl` | 球体 + 平面 + Phong | ✅ |
| `step2_boolean_operations.glsl` | 布尔运算 + Soft Shadow | ✅ |
| `step3_final_scene.glsl` | 综合场景 "The Emergence" | ✅ |

## 如何使用
1. 打开 [shadertoy.com](https://www.shadertoy.com/)
2. 点击 "New"
3. 将任一 `.glsl` 文件的内容粘贴到编辑器中
4. 点击播放按钮 ▶️
